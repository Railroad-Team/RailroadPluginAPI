package dev.railroadide.railroadpluginapi;

/**
 * ExtensionPoint is a marker interface used to define extension points in the Railroad Plugin API.
 * <p>
 * Extension points are interfaces that plugins can implement to extend the functionality of the Railroad system.
 * By implementing this interface, a class can be recognized as an extension point, allowing other plugins
 * to register their extensions against it.
 */
public interface ExtensionPoint {
}
